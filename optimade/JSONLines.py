from typing import Union

from optimade.models.partialdata import PartialDataResponse


def to_JSONL(input: Union[list[dict], PartialDataResponse]):
    """This function convert a list of dictionaries to the JSONL format which can be send back in an OPTIMADE partial data response"""
    pass
